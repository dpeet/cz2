# Mountain House Thermostat - Local Development Configuration

# Backend API Base URL - REMOVED: now auto-derives from window.location.hostname
# Works for both SSH port-forward (localhost) and Tailscale direct access
# VITE_API_BASE_URL=http://localhost:8000

# MQTT WebSocket URL - REMOVED: now auto-derives from window.location.hostname  
# Works for both SSH port-forward (localhost) and Tailscale direct access
# VITE_MQTT_WS_URL=ws://localhost:9001

# API Request Timeout (milliseconds)
VITE_API_TIMEOUT_MS=35000
