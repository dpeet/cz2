# Mountain House Thermostat - Frontend Configuration
#
# Vite automatically loads environment files based on mode:
# - `yarn dev` loads .env.development
# - `yarn build` loads .env.production
# - .env.local overrides both (not committed to git)
#
# For custom setups, copy this file to .env.local and modify

# Backend API Base URL
# Development: http://localhost:8000 (local cz2 API server)
# Production: https://api.mtnhouse.casa (Caddy reverse proxy)
VITE_API_BASE_URL=http://localhost:8000

# MQTT WebSocket URL
# Development: ws://localhost:9001 (local Mosquitto WebSocket)
# Production: wss://mqtt.mtnhouse.casa (Caddy reverse proxy)
VITE_MQTT_WS_URL=ws://localhost:9001

# API Request Timeout (milliseconds)
# Default: 5000ms (5 seconds)
VITE_API_TIMEOUT_MS=5000
